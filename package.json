{
  "name": "converto-business-os",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd frontend && npm run build && cp -r .next ../.next",
    "start": "npm --prefix frontend run start",
    "dev": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "lucide-react": "^0.548.0"
  }
}
